{
	"name": "@prdctr-foundation/network-constants",
	"version": "0.2.1",
	"description": "Single source of truth for PRDCTR network coordinates: SS58 prefix, token decimals/symbol, WS endpoints, explorer base URLs and deep-link builders",
	"license": "UNLICENSED",
	"private": false,
	"publishConfig": {
		"registry": "https://npm.pkg.github.com"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/PRDCTR-Foundation/prdctr-common.git",
		"directory": "packages/chain/network-constants"
	},
	"main": "lib/index.js",
	"types": "lib/index.d.ts",
	"files": [
		"lib/",
		"README.md"
	],
	"engines": {
		"node": ">=20"
	},
	"scripts": {
		"build": "rm -rf lib *.tsbuildinfo && tsc",
		"test": "npm run build && node --test 'test/*.test.mjs'",
		"types:check": "tsc --noEmit",
		"typecheck": "tsc --noEmit",
		"clean": "rm -rf lib *.tsbuildinfo"
	},
	"devDependencies": {
		"@types/node": "catalog:",
		"typescript": "catalog:"
	}
}
